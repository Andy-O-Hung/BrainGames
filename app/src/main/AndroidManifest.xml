<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.BrainGames.BrainGamesComposition">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".ColorMemoryGame" />
        <activity android:name=".MathActivity" />
        <activity android:name=".MathAdditionActivityV2" />
        <activity android:name=".MathSubtractActivity" />
        <activity android:name=".MemoryActivity" />
        <activity
            android:name=".MathMultiplicationActivity"
            android:label="@string/title_activity_math_multiplication"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ReactionActivity" />
        <activity android:name=".MathDivisionActivity" />
        <activity android:name=".LetterMemoryActivity" />
        <activity android:name=".MathCombinationActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>